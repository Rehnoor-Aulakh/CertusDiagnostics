services:
  nginx:
    image: nginx:alpine
    container_name: certus-proxy
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - client
      - admin
      - backend
