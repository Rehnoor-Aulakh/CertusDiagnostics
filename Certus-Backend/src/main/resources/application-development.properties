# Development Configuration Overrides
# This file is loaded automatically when SPRING_PROFILES_ACTIVE=development

# Use localhost URLs in development so that links in emails work locally
app.base-url=http://localhost:5174
app.client-url=http://localhost:5173

# You can also add other development-specific overrides here, like:
# spring.jpa.show-sql=true
